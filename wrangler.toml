name = "oneamlog"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Assets (static files)
assets = { directory = "./public", binding = "ASSETS" }

# ローカル開発用のポート設定
[dev]
port = 8787

# Public variables (non-sensitive)
[vars]

# Secret variables (use: wrangler secret put VARIABLE_NAME --env="")
# SUPABASE_URL
# SUPABASE_ANON_KEY
# SUPABASE_SERVICE_ROLE_KEY
